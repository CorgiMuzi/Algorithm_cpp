#include <iostream>
#include <vector>

using namespace std;
int N = 0, M = 0;
vector<int> inputs[32001];
int lineParent[32001];

int main() {
	cin >> N >> M;

	int A = 0, B = 0;
	for (int i = 0; i < M; ++i) {
		cin >> A >> B;
		
	}
}