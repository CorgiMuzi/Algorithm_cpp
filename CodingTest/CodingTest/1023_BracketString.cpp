#include <iostream>

using namespace std;

typedef long long lld;
// nbstr[a][b] = a 길이의 문자열이 b 값을 가질 때 괄호 ㄴㄴ 문자열의 갯수
int nbstr[51][101];
int N;
lld K;

int main() {
	cin.tie(nullptr);
	ios_base::sync_with_stdio(false);

	cin >> N >> K;
	nbstr[0][50] = 1;
	for(int i = 1; i <= N; ++i) {
		
	}
}